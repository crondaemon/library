<main class="container">
  <%= link_to sanitize("&larr; Back to book"), book_path(@book) %>

  <div class="header">
    <h1>Edit book</h1>
  </div>

  <%= turbo_frame_tag @book do %>  
    <%= render "form", book: @book %>
  <% end %>
</main>